<h1><%= link_to(@project.name, projects_path) %></h1>
<ul>
<% @entries.each do |entry| %>
<li>
<%= entry.hours %> h
<%= entry.minutes %> m-
<%= entry.date %>
</li>
<% end %>
</ul>
<%= @hours %> total hours worked